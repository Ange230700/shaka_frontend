# .husky\pre-push

echo "🔎 Running unit tests before push..."

# Ask Jest what tests exist; run only if there are some
if npx jest --listTests | grep -q .; then
  npm test
else
  echo "🟡 No tests found — skipping Jest."
fi
